{
  "name": "mockapi",
  "version": "1.0.0",
  "description": "mockapi",
  "main": "server.js",
  "scripts": {
    "api": "json-server ./db.js --middlewares ./middlewares.js --port 3000 --static ./dist/",
    "devapi": "json-server ./db.js --middlewares ./middlewares.js --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.15.0"
  },
  "author": "",
  "license": "ISC"
}
